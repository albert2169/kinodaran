import 'package:flutter/material.dart';

class SectionStartContainer extends StatelessWidget {
  const SectionStartContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 2,
      height: 30,
      decoration: BoxDecoration(
        color: Color(0xFFF9EE11),
      ),
    );
  }
}
